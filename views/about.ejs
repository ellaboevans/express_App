<!DOCTYPE html>
<html lang="en">
  <%- include("./partials/head.ejs") %>
  <!-- <style>
    * {
      box-sizing: border-box;
      padding: 0;
      margin: 0;
    }

    nav {
      background-color: #971f1f;
      color: #fff;
      padding: 1.5rem;
      display: flex;
      justify-content: center;
      gap: 1rem;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
    }

    nav a {
      color: #fff;
      text-decoration: none;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      font-weight: 500;
      transition: all 0.3s ease-in-out;
    }
    nav a:hover {
      color: #ccc;
    }
  </style> -->
  <body>
    <%- include("./partials/nav.ejs")%>
    <section id="home-sec">
      <article class="home">
        <h1>About Page</h1>
        <% if (paragraphs.length > 0) {%> <% paragraphs.forEach(paragraph => {%>
        <p><%= paragraph.snippet%></p>
        <%})%> <%}else {%>
        <p>There are no information to display.</p>
        <%}%>
        <div>
          <img
            src="https://images.pexels.com/photos/2379004/pexels-photo-2379004.jpeg?auto=compress&cs=tinysrgb&w"
            alt="person-smiling"
            width="100"
            height="100"
          />
        </div>
      </article>
    </section>
    <%- include("./partials/footer.ejs")%>
  </body>
</html>
